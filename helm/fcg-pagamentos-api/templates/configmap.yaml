{{- if .Values.configmap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fcg-pagamentos-api.fullname" . }}-config
  labels:
    app: {{ include "fcg-pagamentos-api.name" . }}
data:
{{- range $key, $value := .Values.configmap.data }}
  {{ $key }}: "{{ $value }}"
{{- end }}
{{- end }}