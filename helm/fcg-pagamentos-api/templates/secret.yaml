{{- if .Values.secret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "fcg-pagamentos-api.fullname" . }}-secret
  labels:
    app: {{ include "fcg-pagamentos-api.name" . }}
type: Opaque
data:
{{- range $key, $value := .Values.secret.data }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
